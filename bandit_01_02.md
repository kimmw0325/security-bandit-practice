# Level 1 -> 2

## 목표
//

## 사용한 명령어
```bash
//
```

### 실행 과정
ssh로 bandit 서버에 원격 연결

```bash
ls
```
현재 디렉토리에 어떤 파일이 있는지를 확인

- 파일이 있는 것을 확인

```bash
cat -
```
cat 명령어로 파일 내용을 출력

비밀번호가 출력되었으므로 
```bash
exit
```
